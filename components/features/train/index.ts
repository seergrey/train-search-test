/** Train detail feature: the booking flow and the live seat counter around it. */
export { BookingForm } from './booking-form';
export { SeatsBadge } from './seats-badge';
export { SeatsLeftProvider, useSeatsLeft } from './seats-left-context';
